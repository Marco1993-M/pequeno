export const metadata = {
  title: "Discuss Your Project | Pequeño",
  description: "Tell Pequeño about your bespoke home or lightweight steel frame project. Share your location, intended scope and budget to start a conversation.",
  alternates: { canonical: "/onboarding" },
};

export default function OnboardingLayout({ children }) {
  return children;
}
